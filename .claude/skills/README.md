# Claude Code Skills

本项目的自定义技能集合。

## 可用技能

### 📦 /release - 版本发布

自动化完整的版本发布流程。

**用法**:
```bash
# 交互式发布（推荐）
/release

# 直接指定版本类型
/release patch   # 1.1.5 -> 1.1.6 (bug 修复)
/release minor   # 1.1.5 -> 1.2.0 (新功能)
/release major   # 1.1.5 -> 2.0.0 (破坏性更新)
```

**流程**:
1. ✅ 检查 Git 状态
2. 📝 询问版本类型和提交信息
3. 💾 提交所有变更
4. 📦 升级版本号（修改 package.json）
5. 🏷️ 创建 Git tag
6. 🚀 推送代码和标签到 GitHub
7. ⚡ 自动触发 GitHub Actions（创建 Release + 发布到 NPM）

**何时使用**:
- 准备发布新版本
- 需要创建 GitHub Release
- 需要发布到 NPM

---

### 🚀 /push - 快速推送

快速提交并推送代码，不升级版本。

**用法**:
```bash
# 交互式推送
/push

# 直接指定提交信息
/push "fix: 修复配置切换问题"
/push "docs: 更新文档"
```

**流程**:
1. ✅ 检查 Git 状态
2. 📝 获取提交信息
3. 💾 提交所有变更
4. 🚀 推送到 GitHub

**何时使用**:
- 日常开发提交
- 临时保存进度
- 不需要发布新版本的提交

---

## 使用示例

### 场景 1: 修复了一个 bug，准备发布

```bash
# 使用 release skill
/release patch

# 输入提交信息: fix: 修复环境变量检查逻辑
# 确认发布: y
# ✓ 自动发布到 NPM
```

### 场景 2: 添加了新功能，准备发布

```bash
# 使用 release skill
/release minor

# 输入提交信息: feat: 添加自动激活和环境变量检查
# 确认发布: y
# ✓ 自动发布到 NPM
```

### 场景 3: 只想提交代码，暂不发布

```bash
# 使用 push skill
/push "chore: 更新文档"

# 确认推送: y
# ✓ 仅推送代码，不会发布
```

---

## 技术实现

Skills 位于 `.claude/skills/` 目录：

```
.claude/skills/
├── README.md          # 本文档
├── release.md         # release skill 说明
├── release.sh         # release skill 实现
├── push.md            # push skill 说明
└── push.sh            # push skill 实现
```

每个 skill 包含：
- `.md` 文件：技能说明文档
- `.sh` 文件：Shell 脚本实现

---

## 版本号规范（Semantic Versioning）

格式: `MAJOR.MINOR.PATCH`

- **MAJOR** (主版本号): 破坏性更新，不向后兼容
  - 示例: 1.1.5 -> 2.0.0
  - 使用: `/release major`

- **MINOR** (次版本号): 新功能，向后兼容
  - 示例: 1.1.5 -> 1.2.0
  - 使用: `/release minor`

- **PATCH** (补丁版本号): Bug 修复，向后兼容
  - 示例: 1.1.5 -> 1.1.6
  - 使用: `/release patch`

---

## 提交信息规范（Conventional Commits）

推荐使用以下前缀：

- `feat:` - 新功能
- `fix:` - Bug 修复
- `docs:` - 文档更新
- `style:` - 代码格式调整（不影响功能）
- `refactor:` - 重构
- `perf:` - 性能优化
- `test:` - 测试相关
- `chore:` - 构建工具、辅助工具变动

示例：
```
feat: 添加自动激活配置功能
fix: 修复环境变量检查逻辑
docs: 更新 README 使用说明
chore: 添加 GitHub Actions 工作流
```

---

## 常见问题

**Q: release 和 push 有什么区别？**

A:
- `release`: 完整的发布流程，包括版本升级、创建 tag、触发 NPM 发布
- `push`: 仅提交和推送代码，不会升级版本

**Q: 如何撤销错误的版本发布？**

A:
```bash
# 1. 删除本地 tag
git tag -d v1.1.6

# 2. 删除远程 tag
git push origin :refs/tags/v1.1.6

# 3. 回退 package.json 版本
# 手动编辑或使用: npm version <old-version> --no-git-tag-version
```

**Q: 发布失败怎么办？**

A: 检查以下几点：
1. 是否配置了 NPM_TOKEN secret
2. GitHub Actions 日志中的错误信息
3. NPM 账号是否有发布权限
4. 版本号是否已存在

---

## 配置要求

发布到 NPM 需要配置 Secret：

1. 访问 https://www.npmjs.com/settings/YOUR_USERNAME/tokens
2. 创建 Automation Token
3. 在 GitHub 仓库设置中添加 `NPM_TOKEN` secret

路径: `Settings` → `Secrets and variables` → `Actions` → `New repository secret`
